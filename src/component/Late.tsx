import * as React from 'react';
import { Text, View, StyleSheet } from 'react-native';

interface Props {}

const Late = (props: Props) => {
  return (
    <View style={styles.container}>
      <Text>ok</Text>
    </View>
  );
};

export default Late;

const styles = StyleSheet.create({
  container: {}
});
